# Production/hosted DB (Railway Postgres)
DATABASE_URL="postgresql://<user>:<password>@<host>:<port>/<db>?sslmode=require"

# Optional local-only fallback (uncomment for local sqlite testing)
# DATABASE_URL="file:./dev.db"
